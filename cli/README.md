# PWASM Command-line Interface

This directory contains the code for [PWASM][] command-line
interface (CLI)

Commands are stored in the `cmds/` directory, and tests are stored in
the `tests/` directory.

[pwasm]: https://pwasm.org/

#include "pwasm-compile.h"

void *
pwasm_compile(
  pwasm_env_t *env,
  const pwasm_mod_t *mod,
  const size_t func_ofs
) {
  (void) env;
  (void) mod;
  (void) func_ofs;

  // return FAILURE
  return NULL;
}
